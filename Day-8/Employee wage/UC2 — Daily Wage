public class UC2_DailyWage {
    public static void main(String[] args) {

        int wagePerHour = 20;
        int fullTime = 8;

        int check = (int)(Math.random() * 2);

        if (check == 1)
            System.out.println("Salary: " + (fullTime * wagePerHour));
        else
            System.out.println("Salary: 0");
    }
}
